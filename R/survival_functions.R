#'@title Compute KDE and JSD
#' 
#' Compute kernel density estimate (KDE) of a marker expression and 
#' Jensen Shannon distance between two computed KDEs
#'
#' @param x is a list of marker expression values in different cells of a patient
#' @param m is the number of grids used in KDE, default is m = 1024
#' @param px is the KDE of first marker
#' @param py is the KDE of second marker
#' @return The function dens_univ returns KDE and the function jensen_shannon_dist returns the JSD between two densities
#' @export

coxPH_plot = function(x){
  
}


coxme_model = function(x){
  
}